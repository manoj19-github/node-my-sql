import isEmailDuplicate from "./checkEmailDuplicate.middleware"
import checkValidator from "./checkValidator.middleware"
const middlewares={
    isEmailDuplicate,
    checkValidator
}
export default middlewares